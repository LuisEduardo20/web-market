// Cypress support file for e2e tests
// This file is processed and loaded automatically before your test files

beforeEach(() => {
  // Reset any application state before each test if needed
});

afterEach(() => {
  // Clean up after each test if needed
});
